{:
    [(&Int32, &Int32) -> Int32]
        ref_sum;
    
    [(Int32, Int32) -> Int32]
        sum;

    [(End) -> (IO)]
        main;
:}

std::debug;
std::putInt32;

sum := (a, b) -> a + b;
ref_sum := (a, b) -> sum(a, b);

main := () -> putInt32(debug(), ref_sum(2, 3));
